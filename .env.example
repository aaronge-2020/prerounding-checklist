# This static local-first app does not require backend credentials.
#
# Workup Studio proposals are submitted through GitHub Issues/PRs using
# exported workup_change_set_v1 JSON. Configure the target repository in
# index.html if this project is forked.
